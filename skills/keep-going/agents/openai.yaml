interface:
  display_name: "Keep Going"
  short_description: "Keep Codex moving on long tasks until truly blocked"
  default_prompt: "Use $keep-going and keep executing until the task is done or a real blocker appears."

policy:
  allow_implicit_invocation: true
